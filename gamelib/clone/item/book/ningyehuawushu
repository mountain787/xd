#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_ITEM;
protected void create(){
	name=object_name(this_object());
	name_cn="【秘典】凝液化物术";
	desc="记载着凝液术和化物术的秘密\n";
	desc+="[学习:spec_learn "+name+"]\n";
	unit="本";
	picture=name;
	set_item_task(1);
	set_item_canEquip(0);
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(0);
	set_item_canSend(0);
	set_item_canStorage(1);
}
