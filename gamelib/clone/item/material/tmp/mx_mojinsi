#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_COMBINE_ITEM;
protected void create(){
	name=object_name(this_object());
	name_cn="摩金丝";
	unit="根";
	desc="魔线，可以在你的裁缝和制甲中附加100点幸运\n";
	value=1000;
	amount=1;
	picture=name;
	set_item_level(9);
	set_item_canEquip(0);
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	set_for_material("moxian");
	set_add_luck(100);
}
