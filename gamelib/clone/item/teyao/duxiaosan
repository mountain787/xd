#include <globals.h>
#include <gamelib/include/gamelib.h>
inherit WAP_DANYAO;
protected protected protected void create(){
	name=object_name(this_object());
	name_cn="毒消散";
	unit="瓶";
	desc="增加毒素抗性80点，持续30分钟， 下线后效果不消失\n";
	set_danyao_kind("te_defend");
	set_danyao_type("dusu_defend");
	set_effect_value(80);
	set_danyao_timedelay(1800);
	set_item_canDrop(1);
	set_item_canGet(1);
	set_item_canTrade(1);
	set_item_canSend(1);
	set_item_canStorage(1);
	set_item_level(1);
}
